# =============================================================================
# Teu-Im Web App - Environment Configuration
# =============================================================================
# Copy this file to .env.local and fill in the values below.
#
# Required variables are marked with [REQUIRED].
# Optional variables are marked with [OPTIONAL].
# =============================================================================

# --- Supabase (백엔드 & 인증) ---
# Supabase Dashboard > Settings > API 에서 확인
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co   # [REQUIRED]
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here                # [REQUIRED]
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here             # [REQUIRED] 서버 전용 - 클라이언트에 노출 불가

# --- 외부 API ---
# Soniox 실시간 음성 인식 API (사용자별로 앱 내에서 등록하지만, 초기 테스트용으로 필요)
# 참고: 실제 키는 DB 사용자 프로필에 저장됨 (설정 페이지에서 입력)
# SONIOX_API_KEY=your_64_char_hex_key  # [OPTIONAL] 개발 테스트용 (64자리 hex)

# --- Next.js 앱 구성 ---
# 프로덕션 배포 시 실제 도메인으로 변경
NEXT_PUBLIC_APP_URL=http://localhost:3000                        # [OPTIONAL] CORS / 리다이렉트 기준 URL

# --- Security ---
# Comma-separated list of allowed CORS origins (프로덕션 환경에서만 필요)
# 예: https://yourdomain.com,https://www.yourdomain.com
# 개발 환경에서는 localhost가 자동으로 허용됨
ALLOWED_ORIGINS=                                                  # [OPTIONAL] 프로덕션 CORS 설정

# --- Error Tracking ---
# 에러 추적을 위한 외부 엔드포인트 (Sentry, Datadog 등)
# 설정하지 않으면 개발 환경에서는 콘솔에만 로깅됨
# NEXT_PUBLIC_ERROR_ENDPOINT=https://your-error-tracking-endpoint.com/api/errors  # [OPTIONAL] 에러 추적 엔드포인트

# --- Analytics ---
# Web Vitals 성능 지표를 전송할 엔드포인트
# 설정하지 않으면 개발 환경에서는 콘솔에만 로깅됨
# NEXT_PUBLIC_ANALYTICS_ENDPOINT=https://your-analytics-endpoint.com/api/vitals  # [OPTIONAL] Web Vitals 분석 엔드포인트
